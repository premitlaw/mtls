package com.capgemini.demo.config;

public class JksGen {

  //  public static void importPemCertIntoDestStore(String certName, String destName) throws
  // Exception {
  //    String cmd =
  //        String.format(
  //            "-import -file %s -alias server -keystore %s -keypass password -storepass password
  // -noprompt",
  //            certName, destName);
  //    execute(cmd);
  //  }
  //
  //  private static void execute(String command) throws Exception {
  //    sun.security.tools.keytool.Main.main(parse(command));
  //  }
  //
  //  private static String[] parse(String command) {
  //    return command.trim().split("\\s+");
  //  }
}
